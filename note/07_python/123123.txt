min() : 문자열에도 사용 가능 
 ㄴ 함수의 주어진 입력값들 중에 가장 작은 값을 반환
    ㄴ 여러 개의 값을 비교하거나, 리스트, 튜플 등 반복 가능한 객체에서 최소값을 구할때 사용 
max()
 ㄴ 함수의 주어진 입력값들 중에서 가장 큰 값을 반환
    ㄴ min()과 비슷하게 사용되며 최대값을 구할대 사용
mean()
 ㄴ 함수의 주어진 입력값들 중 평균값을 계산하는 함수
std()
 ㄴ 표준편차를 계산하는 함수로 NumPy 라이브러리에서 사용